<h1>Testing</h1>

<p>Adding elements to an array as last</p>

<code>
array = array.push("element-to-push");
</code>

<p> Adding elements as first to an array</p>
<code>
array = array.unshift("element-to-unshift");
</code>
<p> Deleting  element from an array</p>
<code>
  array = array.pop("element-to-remove");
</code>

<h2>Recorrido de arrays</h2>
<p>Array filter (search for arrays meeting the established criteria)</p>
<code>
  array.filter(function(){
    return item.value = 100;
  })
</code>

<?php include "footer.php"; ?>
